/**
 * comcheck.js
 */
var assert = require('assert');

describe('comcheck', () => {
    /*
     * TODO: Test comcheck
     * Automatically register all checks
     * If calling unregister first, register all checks first
     * Register/Unregister
     * Inform reporter if exception thrown
     * Create a message from an array
     */
});
